今天学的内容还是比较简单的，主要学了http的请求过程，首先是先把请求体序列化成json,然后创建https请求的内容，包括URL,请求头，请求体，然后用.do的方法正式开始请求，获取响应之后解析请求头中的状态码和json数据，把json数据解析成go结构体，然后返回给调用方。

还学了接口的底层是长什么样的，第一个参数是类型，第二个参数是具体传进来的数据。在别人调用这个接口的时候就看第一个参数中有没有实现接口中的方法，有的话就会调用这个方法，没有的话就会报错。